# Runtime dependencies (installed in the Docker image).
# Test/eval tooling: requirements-dev.txt
fastapi==0.139.1
uvicorn[standard]==0.51.0
SQLAlchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.18.5
greenlet==3.5.3
pydantic==2.13.4
pydantic-settings==2.14.2
httpx==0.28.1
openai==2.45.0
retell-sdk==5.55.0
python-dotenv==1.2.2
# Patient-name integrity gate (app/services/names.py): Devanagari→Latin
# romanization + fuzzy roster matching against ASR-mangled names.
indic-transliteration==2.3.82
rapidfuzz==3.14.5
# IANA timezone database — REQUIRED on Windows, where Python has no system tz
# data (zoneinfo raises ZoneInfoNotFoundError for Asia/Kolkata without it).
tzdata==2025.2
