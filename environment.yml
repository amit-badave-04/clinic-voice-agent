# Local development environment (deployment uses Docker + requirements.txt).
# Create with:  conda env create -f environment.yml
name: voice-ai-agent
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - -r requirements.txt
      - -r requirements-dev.txt
